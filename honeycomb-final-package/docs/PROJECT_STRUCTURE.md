# 🐝 벌집 구조 학습 시스템 - 프로젝트 구조

## 📁 전체 파일 구조

```
honeycomb-learning-system/
│
├── 📂 핵심 파일 (Core)
│   ├── honeycomb_integrated_app.py    ⭐ 최종 통합 버전 (Streamlit)
│   ├── honeycomb_learning_system.py      기존 학습 엔진 (84개 유니트)
│   └── test_honeycomb_standalone.py      테스트 코드 (28개 테스트)
│
├── 📂 UI 앱 (Applications)
│   ├── honeycomb_app.py                  기존 Streamlit 앱
│   └── honeycomb_visual_app.py           벌집 시각화 Streamlit 앱
│
├── 📂 React 데모 (Artifacts)
│   ├── honeycomb_final.jsx            ⭐ 최종 React 버전 (5가지 적합성)
│   ├── honeycomb_interactive.jsx         인터랙티브 React 데모
│   └── honeycomb_demo.jsx                기본 React 데모
│
├── 📂 데이터 (Data)
│   ├── honeycomb_units_final.csv         61개 유니트 (벌집용)
│   ├── honeycomb_units_final.json        61개 유니트 JSON
│   ├── honeycomb_units_84.csv            84개 유니트 (기존)
│   └── honeycomb_units_84.json           84개 유니트 JSON
│
├── 📂 문서 (Documentation)
│   ├── README.md                         프로젝트 가이드
│   ├── honeycomb_system_documentation.docx  기술 문서
│   └── PROJECT_STRUCTURE.md              이 파일
│
├── 📂 설정 (Configuration)
│   └── requirements.txt                  Python 패키지 목록
│
└── 📂 배포 (Distribution)
    └── honeycomb-project.zip             전체 패키지 (기존 버전)
```

---

## 🎯 파일별 역할

### ⭐ 핵심 파일 (사용 권장)

| 파일 | 설명 | 특징 |
|------|------|------|
| **honeycomb_integrated_app.py** | 최종 통합 버전 | 벌집 시각화 + 5가지 적합성 + 6개 생성정보 |
| **honeycomb_final.jsx** | React 아티팩트 | 웹에서 바로 실행 가능 |
| **test_honeycomb_standalone.py** | 테스트 코드 | 28개 테스트, 의존성 없음 |

### 📊 버전 비교

| 버전 | 유니트 수 | 시각화 | 적합성 점수 | 생성정보 |
|------|----------|--------|------------|----------|
| 기존 (84개) | 84개 | ❌ | ✅ 5가지 | ✅ 6개 |
| 통합 (61개) | 61개 | ✅ 벌집 | ✅ 5가지 | ✅ 6개 |

---

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────┐
│                        벌집 학습 시스템                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐          │
│  │  학습자       │    │  유니트       │    │  시뮬레이터   │          │
│  │  프로필       │ ×  │  고정정보     │ →  │              │          │
│  │              │    │              │    │              │          │
│  │ • 성향축 4가지│    │ • 난이도 1~12│    │ • 생성정보   │          │
│  │ • 난이도반응  │    │ • 유니트타입  │    │   6개 필드   │          │
│  │ • 미디어선호  │    │ • 미디어      │    │              │          │
│  │ • 몰입이탈   │    │ • 선행조건    │    │ • 적합성점수 │          │
│  │ • 행동성향   │    │ • 인접셀      │    │   5가지      │          │
│  └──────────────┘    └──────────────┘    └──────────────┘          │
│         │                   │                   │                   │
│         └───────────────────┼───────────────────┘                   │
│                             │                                       │
│                             ▼                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                      벌집 시각화 (61셀)                        │  │
│  │                                                              │  │
│  │     🟢 완료  🟠 현재  🟣 추천  🔵 가능  ⚪ 잠김              │  │
│  │                                                              │  │
│  │              [나선형 육각형 맵]                               │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📋 데이터 구조

### 1️⃣ 학습자 프로필 (5가지 카테고리)

```python
LearnerProfile:
├── 성향축 (합계 100)
│   ├── 탐험형: int
│   ├── 성취형: int
│   ├── 경쟁형: int
│   └── 창조형: int
│
├── 난이도 반응
│   ├── 도전_선호도: 낮음/중간/높음
│   └── 실패_인내도: 낮음/중간/높음
│
├── 미디어 선호 (0~1)
│   ├── 미디어_이미지: float
│   ├── 미디어_텍스트: float
│   ├── 미디어_숫자: float
│   └── 미디어_영상: float
│
├── 몰입·이탈 특성
│   ├── 평균_집중_지속시간_초: int
│   ├── 지루함_임계치_초: int
│   └── 이탈_임계치_실패횟수: int
│
└── 행동 성향
    ├── 재도전_확률: int (0~100)
    ├── 확장_선택_확률: int (0~100)
    └── 휴식_수용도: 낮음/중간/높음
```

### 2️⃣ 유니트 고정정보 (61개 벌집 셀)

```python
HexUnit:
├── cell_id: int (1~61)
├── unit_type: 개념/실전/탐색/보조
├── difficulty: int (1~12)
├── subject: str (A~G)
├── subject_name: str
├── prereq_required: List[int]
├── prereq_recommended: List[int]
├── adjacent_cells: List[int]
├── recommended_media: 이미지/텍스트/숫자/영상/혼합
├── estimated_time_sec: int
├── fail_allow: int
├── reward_type: 칭찬/개방/시각효과
├── is_completed: bool
├── is_locked: bool
└── score: float
```

### 3️⃣ 생성정보 (6개 핵심 필드)

```python
LearningLog:
├── 체류시간_초: int          # 유니트에서 머문 시간
├── 실패횟수: int             # 학습 중 실패 횟수
├── 재도전_여부: bool         # 실패 후 재시도 여부
├── 이탈_여부: bool           # 중도 포기 여부
├── 보상반응: str             # 칭찬/개방/시각효과 중 반응
└── 선호미디어_반응점수: dict  # 미디어별 반응 (0~1)
```

### 4️⃣ 5가지 적합성 점수

```python
UnitMatchScore:
├── 난이도_적합성: float (0~1)      # 가중치 25%
├── 학습타입_적합성: float (0~1)    # 가중치 20%
├── 미디어_궁합: float (0~1)        # 가중치 15%
├── 선행조건_충족도: float (0~1)    # 가중치 25%
├── 성향_방향성: float (0~1)        # 가중치 15%
└── total_score: float             # 가중 평균
```

---

## 🔄 학습 흐름

```
1. 학습자 생성
   └── 랜덤 프로필 생성 (5가지 카테고리)

2. 셀 1번 학습 (중앙)
   └── 고정정보 × 프로필 → 생성정보 6개 필드

3. 프로필 업데이트
   └── 생성정보 기반 미세 조정

4. 인접 셀 잠금 해제
   └── 완료 시 주변 셀 열림

5. 다음 셀 추천
   └── 5가지 적합성 점수 계산 → Top 3 추천

6. 반복 (2~5)
   └── 61개 셀 완료까지
```

---

## 🚀 실행 방법

### 방법 1: Streamlit (권장)

```bash
# 패키지 설치
pip install streamlit plotly numpy

# 최종 통합 버전 실행
streamlit run honeycomb_integrated_app.py
```

### 방법 2: 테스트 실행

```bash
# 의존성 없이 바로 실행
python test_honeycomb_standalone.py
```

### 방법 3: React 아티팩트

Claude 대화에서 `honeycomb_final.jsx` 파일을 아티팩트로 열면 바로 체험 가능

---

## 📊 테스트 커버리지

| 테스트 카테고리 | 테스트 수 | 상태 |
|---------------|----------|------|
| 학습자 프로필 | 4개 | ✅ |
| 벌집 유니트 | 7개 | ✅ |
| 생성정보 6개 필드 | 7개 | ✅ |
| 5가지 적합성 점수 | 5개 | ✅ |
| 인접 셀 잠금 해제 | 2개 | ✅ |
| 프로필 업데이트 | 2개 | ✅ |
| 전체 시뮬레이션 | 1개 | ✅ |
| **총계** | **28개** | **✅ 100%** |

---

## 📦 배포 패키지

### 최소 구성 (권장)

```
honeycomb-final/
├── honeycomb_integrated_app.py   # 메인 앱
├── test_honeycomb_standalone.py  # 테스트
├── requirements.txt              # 패키지
└── README.md                     # 가이드
```

### 전체 구성

```
honeycomb-complete/
├── apps/
│   ├── honeycomb_integrated_app.py
│   ├── honeycomb_visual_app.py
│   └── honeycomb_app.py
├── artifacts/
│   ├── honeycomb_final.jsx
│   ├── honeycomb_interactive.jsx
│   └── honeycomb_demo.jsx
├── data/
│   ├── honeycomb_units_final.csv
│   ├── honeycomb_units_final.json
│   ├── honeycomb_units_84.csv
│   └── honeycomb_units_84.json
├── tests/
│   └── test_honeycomb_standalone.py
├── docs/
│   ├── README.md
│   ├── PROJECT_STRUCTURE.md
│   └── honeycomb_system_documentation.docx
├── requirements.txt
└── honeycomb_learning_system.py
```

---

*Last Updated: 2024-12-23*
